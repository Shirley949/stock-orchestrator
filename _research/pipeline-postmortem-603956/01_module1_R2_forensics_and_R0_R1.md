# 交付物② 模块 1：R2 法证（H-A~H-E）+ R0 + R1（含 ROI 清算 transcript 侧）

## 1a H-A「锚块进度行失真」— REFUTED（并自我纠错）

四锚块逐一与勾选台账+磁盘对拍（**先纠正我复盘中途的一次误读**：曾把东材/威派格两锚块张冠李戴判出「章名交换缺陷」——实为跨文件 block 配对错误，撤回）：

| 锚 | 台账实况（L 证据） | 锚块所述 | 裁决 |
|---|---|---|---|
| 东材 c1@L404 | checked={c60,c_m1,c61,c_d2,c_d3,c63(L386)}=6；四 on disk@L348、c62 未勾（L408 才补） | 已写 6/16（…五、技术面）｜当前章 c62 四 → 补跑④→⑤ | ✅ 忠实（四=盘上在场未勾，补跑④⑤正确） |
| 威派格 c1@L409 | checked 含 c62（L378 复合命令 VG --section 四、订单 + --check c62；L379 回显「进度: 6/51」） | 已写 6/16（…四、订单）｜当前章 c63 五 → 从①重跑 | ✅ 忠实（五 mid-flight：m3 已读 L383、视图已拉 L385-395、未 append） |
| 东材 c2@L667 | c_d4 未落账（L605 复合未全中，L721 才重勾） | 已写 13/16｜待写 {10.1,八,速览} | ✅ 忠实 |
| 威派格 c2@L807 | 第 16 勾=c68b@L716 | 已写 16/16 → 走 c70 | ✅ 忠实（但「中枢: m6 未写」=假阴性，见 R4①） |

**结论：进度行机制（勾选账×盘上锚+D3 异常态，c2_compact_inject.py L83/L217-236）四/四忠实。T1 的前提（进度行误导）不成立，降级为措辞打磨（P2）。**

## 1b H-B「compact 后一切需 Read 的指针皆死」— 两分

- **数据面指针全活**：post-compact `--section` gate×33、update_checklist、步定义 grep（威派格 7 次：L462/465/599/677/716/727/871）、逐步②视图——锚定行为整体未崩。
- **模块文件指针全死（威派格）**：compact#1(L409) 后 **8 个首写章（六七八·十一·十·九·十二·八）模块 Read = 0**（全转录 Read modules/ 扫描：末次 m3@L383 pre-compact）。关键加重情节：**「① 读 modules/mX.md」指令至少 4 次被 grep 进 context（L465 c64/L599 c66/L677 c67/L716 c68b），仍然跳过**——in-context 指令 ≠ 执行。根因=模块读是唯一无下游机械消费的步动作（gate 查报告文本、不查读经）→ **T11 机械阻抗获批，正反两极 fixture 齐备**（威派格 0/8 vs 东材 16/16，同一引擎同一锚文案）。

## 1c H-C/load_skeleton 矛盾裁决（用户点名先答）— **实现缺口 CONFIRMED，T11 有真数据源**

- **设计**：plan v1.1 L173「骨架勾选表（**hook 内跑 load_skeleton，消费链自动闭合**——批4 消费✗ 的修法）」+ L214「C2 锚块升级（内联当前章命令/load_skeleton **自动跑**/裁剪次序）」。
- **实现**：c2_compact_inject.py L176-180 只**拼命令字符串**注入（`骨架翻页=load_skeleton.py {sk} --transcript {tp}`），无任何 subprocess 执行；全文件 grep "skeleton" 仅 3 处、全是字符串拼接。
- **实测**：两票全程 load_skeleton/analysis_skeleton 调用 = **0 次**（威派格/东材全转录扫描）。骨架文件本体存在（威派格 generate_checklist 带 `--skeleton-out` 生成，L26）。
- **裁决**：批2 设计了「自动跑」、实现只交付了「打印命令」、模型侧也从未手动跑——三环全断，台账=死信。**T11 不是空中楼阁：数据源（骨架台账文件）真实存在，缺的是消费接线。** 修法两臂：hook 执行 load_skeleton 并**内联输出**（非命令）；和/或 update_checklist ⑤ 勾选时校验模块读台账。

## 1d H-D「compact 摘要冒充模块知识」— 成立（贡献因子，非根因）

L419 thinking：「Per m3-technical.md structure **(from summary)**」——模型明确以 compact 摘要里的模块梗概替代真模块文件写 §五。摘要给了「结构错觉」，§六以后摘要渐薄，退化为步文本+gate 反应式补漏。与 1b 同向：没有读经台账，摘要冒用不可检测。

## 1e H-E「⑤勾选批回填」— 大半 REFUTED

威派格 22 次勾选逐一对齐 append：c63@L462 紧跟 gate 复核（L457-460）、c64@L540/c65@L575/c66@L599/c67@L677 逐章即时；两处「双勾」（c_d4+c_d5@L643、c68+c_m10@L702）与**双章合并 append**（L638、L693）一一对应——批回填跟踪的是 append 合并，不是时间回填。真 backfill 仅东材 L408（c62，T2 案）与 L721（c_d4 重做勾）。**⑤ 纪律基本守住；失守的是 ①（读），不是 ⑤（勾）。**

## 1f R0 预载违例 — 关闭：未发生（0/3 会话）

- 触发考古：L13 thinking 把「tooling-playbook（websearch）与 SOP（发布）」捆绑列为待读（**任务侧无「趁拉取期读 SOP」指令源，属模型自行打包叙述**）；L37/L51 收窄为只读 playbook 且给出合法触发（「任一工具动作前」+「马上要 websearch」）；L69 TaskCreate 把「读 SOP.md」写进 Phase5 描述——**任务清单成了指针载体，穿越两次 compact 存活**。
- 代价：SOP 仅 L886 读一次（3,133B，P5 发布前，L878「发布前强制 Read」），随后 L940 五步闸执行。**单次 JIT、零预载、零重读**——「预载内容在需要时已不在场」情景未发生，因为根本没有预载。
- 系统性对照：东材 4b SOP Read=0（停在发布前待用户确认，L959/L970）；4a/B2（d30dfe07）SOP 命中=0。**0/3 预载，修向政策⑧不触发，无工单。**
- 对照项成立：同刻读 tooling-playbook 合法（L73）、SOP 同刻不读合法（触发表「发布动作前」）——反差证明非一刀切过载，触发语义执行正确。

## 1g R1 退役面残存 + ROI 清算（transcript 侧）

- **引文定谳**：「研报 PDF 3 份 ✓」在 02557899=0 命中、11f32cf4=0 命中；逐字命中于 **ed7c32a8（300450 先导智能直播会话）**：「研报 PDF 3 份 ✓、EPS 一致预期 ✓、合同负债 8 期趋势 ✓（26H1 达 174 亿…）」（assistant text）。融合形态确认：3=layer1 覆盖计数（em_reports_count）与 PDF 步✓在收单叙述里相邻融合 → **narration 漂移（把覆盖数说成 PDF 下载数），非威派格票事件**。威派格侧双账一致性反而是正例：c_pdf_research **未勾**+盘面 0 家三处披露（L321 报告行/L531 §六/L687-693 §八）+L963/L999 thinking 明示「不勾（不满足字面 ≥3 份），真空已披露」。
- **东材 layer2 消费定谳（transcript 侧）**：`any s35_research_reports.data.layer2` 投影调用 = **0**；报告 [src:] 指向 layer2 = **0**（归档报告 grep）。模型仅见过 runner stdout 的 layer2 标题回显（L219/L330 thinking 引述「layer2 1 PDF ok」）并在 L717/723/726/727 花 ~4 轮核验「PDF 到底下载没」——**收益=0 消费字符，成本=72s 场景+518KB 缓存+4 轮核验**。与工件侧四段定性（2/4 噪音+免责声明错位）合账：**自动接线不值，退役或需求门控**（终稿归主会话/用户裁）。
- 附带发现：东材 L150 曾 curl 东材 2025 年报 PDF 入 /tmp/exa_601208（webresearch 流），**下载后零解析零引用**=死下载（小额浪费，记观察）。
- 文档-引擎矛盾（记 T16）：清单步 c_pdf_research 两票均为「PDF 已下载（**≥3份**）」，场景文档 L2=1 份按需；东材在 layer2=1 PDF 实况下勾选该步，按字面读=过度声明。步文本须改为「L1 覆盖≥3 或 L2 按需已取」。

## 1h R4①/R4② 根因定谳（引擎侧，供模块 5）

- **R4①**：c2_compact_inject.py `_t1_scalars` L129 `zh="m6 未写"` 为默认兜底，翻转条件=section_locator 定位的 capstone 切片中 `中枢[^\d%]{0,8}([\d.]+)` 命中。两票 capstone §十一全文**均无「中枢+数字」逐字句**（数字只活在速览对 §11.3 的转引：威派格 L9「中枢约 6.4 元」、东材速览「期望价≈51.1（§11.3）」；两报告「中枢」其余命中全在 §七/§八 宏观语境）→ capstone 已写仍报「m6 未写」=标签把「章节未写」与「中枢读数缺失」混义。假阴性 2/4 锚（东材 c2、威派格 c2），真值 2/4（两票 c1）。
- **R4②**：威派格锚 T1 行原文 `classification=防御股 禁PE框架=?`——snapshot.classification.forbidden_metric 对防御股**未产出**，C2 按无崩溃设计渲染「?」；周期（东材=PE做主要）/成长（300450=PB做主要）均有值。修在上游 classification 场景（T13），不在 C2。
