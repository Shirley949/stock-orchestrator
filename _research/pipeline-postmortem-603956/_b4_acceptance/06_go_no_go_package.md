# 件⑥ GO/NO-GO 放行包（蓝晓 300487 重开）— 批4 独立验收官

> 基调：0b 判据预注册完整性 **PASS**（commit c84bcd2 14:27:16+0800 早于全部运行窗 B2 10:40Z/4a 11:05Z/4b 12:08Z；working tree 终验零 diff；无 NO-GO 直裁事由）。

## 一、GO 五条件逐条核验

| # | 条件 | 状态 | 证据 |
|---|------|------|------|
| 1 | 4a 判据表补齐（三判据×三例逐格三态） | ✅ DONE | 件②（01a4_4a_recovery_windows.txt 重放实录；字面/操作双口径并记，无缺数字格） |
| 2 | m25 三角重建定分支并落字 | ✅ DONE | 件③：(i) 模型没跟 CONFIRMED×2（compact 前 §四 append+全报 gate 过即跳④⑤开§五；compact 后 L407 `--check c62` 跳 mandated ④ 直勾）；(ii) D2 bug REFUTED（进度行=勾选账、三态=磁盘账，`c2_compact_inject.py` 两账并存自洽）；(iii) 措辞 REFUTED（祈使句无歧义） |
| 3 | 判定书抢救归档（主会话执行） | ✅ DONE | commit eab2bab 22:06；`_research/pipeline-batch4-4b-verdict.md` md5=d51088fd == /tmp 原件逐字节一致（84 行） |
| 4 | 判据 v3 冻结（raw 口径修订） | ⏳ 待用户终裁 | 草案=件④（本包第三节同文）；**冻结程序=用户批准后主会话 commit 入 `_research/`，commit 时间戳必须早于蓝晓会话首条消息**；冻结前蓝晓禁跑 |
| 5 | 0c 一致（七组数字无单源） | ✅ DONE | 件①：全部双路确认（transcript 直算 vs token_audit），最大分歧 3%（视图类分子分类边界 75,760 vs 73,551，已解释非矛盾） |

**核验结果：5 条件中 4 DONE + 1 待终裁（该待裁项本身就是放行动作）。无 NO-GO 触发。**

## 二、NO-GO 触发检查（全部未命中）

| 触发器 | 检查法 | 结果 |
|--------|--------|------|
| 运行后改判据 | git 时间戳对拍 + working tree diff | 未触发（判据文件零 diff，终验二次确认） |
| 门数/数字造假 | verified.json 值字段独立重放 | 未触发——B2 `active=12 passed=12 failed=0`（执行者「12/12」CONFIRMED）；4b `active=57 passed=57 failed=0`（「57/57」CONFIRMED）。注：验收官初读曾把顶层 17 个 key 误当门数，系信封结构误读，已自我纠正记录在案 |
| 归档件被改写 | md5 对拍 | 未触发（逐字节一致） |
| 报告凑绿修改 | 4b 报告 87,076B 原样 + 分章 diff 与破线记录一致 | 未触发 |

## 三、判据 v3 草案全文（raw 口径；预注册文本，批准即冻结）

> **v3-r1（raw 调用符合性口径，替代 v1.1 件3「raw≤20%」比例线）**
> 写作会话内 `--raw`/`--field` 直接投影调用，逐次对拍以下白名单，**白名单外调用数 = 0** 为 PASS：
> (a) checklist 步② baked 命令精确匹配（以 `generate_checklist.py` 渲染的命令文本为准）；
> (b) C2 锚③授权章窗投影：当前章原子步①所读模块声明的 snapshot 路径，经 `--field <路径>`/`any <路径>` 取用，且调用时刻处于该章 ①→⑤ 窗内或 compact 恢复窗内（恢复窗=C2 锚块注入时刻→该章⑤勾选时刻）；
> (c) `--list`/存在性 `any` 仪式（C2 续接仪式强制项，恒豁免）。
> 审计固化：token_audit 新增 baked 符合性三列——`baked外调用数`（判据线 0）、`手写提取数`（线 0）、`覆盖率`（线 ≥80%）——三列同表输出，替代原「非 baked=0」与「手写 0/覆盖率」分离口径。
> 废止：raw 输出字节占比 20% 线（v1.1 baked 投影机制下分子由清单指令直接产生，占比随机制成功而变大，量纲失义）。
> 配套记账：v1.1 旧线（raw≤20%）下东材 4b 两破线记录**维持不撤销**（预注册尊严）；v3 自冻结批起生效。

**不对称性声明（用户提醒，验收官背书）**：本修订属「判据被证明量错了对象而修」——14 次调用逐一重放证明 20% 线的分子由清单 baked 指令与 C2 锚③授权构成（0 即兴），尺子量的是机制成功而非行为回潮。本批**没有发生**任何「因结果不好看而修」：长度带在本批一字未动（东材 +15.2% 破线照记），蓝晓判读规则为预注册前瞻文本而非追溯改写。记录保持。

## 四、蓝晓判读规则草案全文（先于数据预注册）

> **v3-r2（长度带 rerun 方差裁决，二票定标规则）**
> 蓝晓 300487 新管线重跑完成后，同口径对比蓝晓旧管线基线（预登记见第五节）：
> - **新报告字节 ≥ 基线 +10%** 且 gate FAIL=0 → 东材 +15.2% 判「架构效应」成立（基线报告被 compact 压抑、逐章 append 释放披露质量的候选签名获第二票）→ 动作：以两票 rerun 分位重定长度带（建议两票均值 ±1 个实测极差），P6 去重审计随行执行；
> - **|Δ| ≤ +5%** → 东材 +15.2% 判「单票方差」→ 旧带保留，东材破线注记改「带定标 n=1 失准」（判据文本不动，只补标定注记）；
> - **+5% ~ +10% 中间带** → 证据不足，二票不定标：跑第三票（同口径）或维持旧带+挂起，禁止折中拍脑袋。
> - 附带判读（同批固化）：分章 Δ×基线 compact 相位相关检验在蓝晓票若可执行（蓝晓基线若亦为单体 Write 则永不可执行，如实记「该检验设计对单体基线先天失效」，不再重试）。
> 本规则自冻结批起生效，**蓝晓结果不得反向修改本规则**（不对称性纪律同 v3-r1）。

## 五、蓝晓基线预登记（928e6ef4，2026-09-15，旧管线，冻结）

| 项 | 值 | 出处 |
|----|-----|------|
| 报告 | **56,086B / 437 行** | `/tmp/analysis_report_300487_modeA.md` |
| gate | profile_full：active 57 / passed 57 / failed 0 / score 100；**修复轮 1·已全过**（verify r1 FAIL→r2 指针行刷新，同 gate_sha e31150bf/0eab3b7a） | verified.json + verify_300487*.log |
| 在册量 v5 | **48,452c@轮134**（构成：视图 42,908 / websearch(预载) 4,806 / runner 477 / xq 261）——旧管线病理量级（判据线 ≤20K） | token_audit 20260916-2235 |
| 总取数 | **69,778c**（CLI 69,611 + 手写 167）；覆盖率 99.8%；真提取 0 | 同上 |
| 探针形状 | 15 章 / **13 拆窗** / m12@84 早读勾@124（旧管线病理正例，与既有记录逐字吻合） | 批3 REFACTOR_LOG |
| compact | 段内手写 1（c1 CLI@+4） | token_audit [v4] |

runbook 程序：①用户终裁 v3-r1/v3-r2 → ②主会话 commit 预注册（时间戳锚定）→ ③蓝晓另起会话 modeA 重跑（同 prompt 口径）→ ④token_audit + atomic_step_probe 同法重放 → ⑤按 v3-r2 判读，判读先于数据。
