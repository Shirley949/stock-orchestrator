# 交付物⑦ 模块 6：蓝晓序裁决包 + 判据 v3 修订草案（终稿供用户终裁）

## 6a 蓝晓序裁决 — 站 fork A（P1 先修，禁先跑）

**理由链**：
1. 蓝晓基线（928e6ef4：56,086B/437行/在册 48,452c/69,778 总取数/13 拆窗）**不可再生**——基线票已按旧引擎烧掉；v3-r2 二票定标只此一次机会。
2. 威派格已经免费充当了「未修复引擎的行为样本」（8/8 post-compact 跳读、4 处锚内指令被无视、T15 活体串票）。若蓝晓在未修复引擎上跑且过程面再失真，长度带判读将被过程噪声污染——**「gate 清白」在旧口径下已被威派格证伪（57/57 与 R1/R2 并存），这正是用户改判据的动因**。
3. P1 五件（T15/T11/T2/T12/T13）全为机械阻抗、fixture 齐备、单项 <1 天——收窄成本远低于烧基线的重置成本（修向政策⑧同向）。

**GO 序**：① 用户终裁 v3-r1+v3-r2（下文修订稿）→ ② 主会话 commit 预注册（时间戳锚定，早于蓝晓首条消息）→ ③ P1 五件落地 + run_regression 契约层全绿 + test_c2_inject 并行用例绿 → ④ 蓝晓另起会话 modeA 同 prompt 重跑 → ⑤ token_audit + probe 重放 → ⑥ 按 v3-r2 判读（判读先于数据，禁反向改规则）。

## 6b 判据 v3-r1（raw 符合性口径）— 批准，无修改

验收官件⑥第三节文本维持：白名单外调用数=0（(a) baked 精确匹配 (b) C2 锚③章窗授权 (c) --list/存在性仪式）；token_audit baked 三列；废止 20% 比例线；东材 4b 两破线记录不撤销。今晚 14 调用重放（0 即兴）再证其量纲成立。**T3 定义随冻结一并落入**（恢复步数=净新数据取数，仪式与④补跑不计，≤3）。

## 6c 判据 v3-r2（长度带 rerun 二票定标）— 批准但按用户指示升级，并给出**可执行定义**

「gate 清白」替换为三列 + 既有两列，逐列定义（今晚法证后可全部机械化重放）：

| # | 列 | 可执行定义（唯一口径） | 蓝晓线 |
|---|---|---|---|
| 1 | 章序单调 | 报告 append/edit 事件序对拍 PHASE3_STEP_ANCHORS 步序非降；单事件多章（merge）记 1 起 note 不断线 | 0 违例 |
| 2 | 模块读完备 | 每写作步 ③ 首写前，转录中存在该步模块文件 Read 事件（endswith 匹配）；redo 用 pre-compact 读=合规但记 stale-redo note | 16/16 |
| 3 | 手写提取 | token_audit 真提取桶（视图内违规+视图外建议，豁免/补救另计） | ≤5 |
| 4 | gate | profile_full FAIL=0（修复轮如实计） | FAIL=0 |
| 5 | 长度带 | vs 基线 56,086B：≥+10% / ±5% / 中间带，三分裁决如验收官件⑥第四节 | 三分规则 |

**东材第一票预填（供参照非标定）**：章序单调✓ 0 merge｜模块读 16/16（stale-redo×1 note）｜手写 0｜57/57。威派格同表（回测票，只作 fixture 不作标定）：单调✓ 2 merge｜5/13｜5｜57/57——两表并排即为 v3-r2 为什么必须换「gate 清白」的一页纸证据。

## 6d T3 定义文本（随冻结 commit 落盘）

> 恢复步数 = compact 锚注入时刻→当前章⑤勾选时刻窗内，**净新数据取数**调用数（snapshot_view 投影/runner 重拉等）；`--list`/存在性 `any` 仪式、③ 前残段删除、④ `--section` 补跑不计。线 ≤3。

## 6e 判据票/回测票分流条款（随 v3 冻结一并入册，见交付物①）

威派格与 300450 的全部数字**永不**进入 v3 标定数据面；只可用作 fixture 与观察层证据。
