# 判据票结案审计（东材 601208 第四跑 · v2 首验）

> 审计官：fork 产出，主会话复验，用户终裁。纪律：零写入（本件例外）；scope 修正只加不减；承重数字全部原始件直算、附重放命令；二阶假说证伪/归因翻转如实判。
> 结论速览：**修正后主判据 PASS（13.09M，对线余量 18.0%）；质量硬线 ✅；程序合规 ✅（两笔已声明/轻注记）——三面全绿，可销案。**
> 一处总裁决前提修正：溯源对照实为 **197 vs 171（v2 +26，+15%）**，非「197 vs 200（−3）」——双源（sidecar+直接计数）一致，见 §4b⑥。

## ① 三元组 + 独立复算 + V1 终验

**1a 三元组补报（transcript 逐 record 累加 == 审计件，差异 0/0/0）**

| 件 | input | cache_read | output | 轮数 |
|---|---|---|---|---|
| v2 判据票 | 747,565 | 35,811,840 | 252,719 | 252 |
| 基线 1M-v1 | 1,369,532 | 68,666,112 | 487,018 | 355 |

取数：总 49,813c（CLI 49,813 + 手写 0），覆盖率 100.0%，**R1 线 ≤85K ✅**。隐性推理：gateway 不回 thinking 明细（`output_tokens_details.thinking_tokens` 恒 0），73% 系审计件推算口径（±30%）——W6 属推算非实测，入账。

**1b EQ 逐位复算**：747,565 + 0.2875×35,811,840（=10,295,904.0）+ 3.5×252,719（=884,516.5）= **11,927,985.5 ≈ 11.9280M** ✅。总裁决反推式 input+3.5×output=1.6321M 确认（747,565+884,516.5）。

同权对照表（公式 input + w×cache + **3.5×**output；v2 审计件敏感性行系 +1×output 口径，两口径并列勿混）：

| 件 | w=0 | w=0.01 | w=0.05 | w=0.1 | w=0.25 | w=0.2875 |
|---|---|---|---|---|---|---|
| 东材4b(200K) | 2.265M | 2.589M | 3.886M | 5.507M | 10.370M | 11.586M |
| 基线 1M-v1 | 3.074M | 3.761M | 6.507M | 9.941M | 20.241M | 22.816M |
| **v2 判据票** | **1.632M** | **1.990M** | **3.423M** | **5.213M** | **10.585M** | **11.928M** |

注：w≳0.187 后 v2 反超 4b（cache 体量 35.8M>32.4M）——同票溢价结构在判据票仍存在，EQ 优势限于 w_real=0.2875 档。

**1c V1 终验（围界法；4 账单端点重试仍 404）**：枚举 5h 窗口（09:53-14:53）全部活跃会话 4 个，双计数假说对表：

| 假说 | Σ窗口 tokens | 隐含限额 L=Σ/0.80 | 反推早期占比（应≈1%） | 判定 |
|---|---|---|---|---|
| **H1 = input+cache+output** | 157,279,454 | ≈196.6M | **0.889%** | ✅ 自洽（0.889% 取整=1% 读数） |
| H2 = input+output | 5,758,814 | ≈7.2M | 10.82% | ❌ 证伪（偏离 10.8×） |

**判定：V1=围界自洽达成**——网关侧独立计量（TOKENS_LIMIT 1%→81% 跳变）与 transcript H1 口径单限额互洽，证明 **cache_read 是真实计入的 tokens**（非 transcript 侧伪影）；0.2875/3.5 价格比率仍以价表侧（已 ✅）为准。回填圣泉/东材 1M-v1：5h 窗口已翻新、无历史窗口绝对量持久化——**不可行**，如实记。

## ② 超额收割归因（EQ 削减 10,887,617 分解）

**2a 分解表（代码实证，逐位闭合）**

| 项 | 量 | 计算式 | 机制证据 |
|---|---|---|---|
| cache 效应 | 9,445,603 | 0.2875×32,854,272 | 见 2b 拆分 |
| ├ 轮次直接（N/P 分解口径） | 5,727,818 | (355−252)×193,426×0.2875 | 轮数 252/355 实测 |
| ├ prefix（N/P 分解口径） | 3,717,786 | (193,426−142,110)×252×0.2875 | ⚠️ 组合效应，见 2b |
| output 效应 | 820,046 | 3.5×(487,018−252,719) | −48.1%：−29% 轮 × −27% 每轮产出 |
| input 效应 | 621,967 | 1,369,532−747,565 | −45.4%，随轮同缩 |
| **合计** | **10,887,617** | = 9,445,603+820,046+621,967 | ✅ 逐位闭合 |

**2b 二阶假说检验：「内容后置」版证伪，「尾部复合」版成立**

- **证伪部分**：同指数 prefix 轨迹基本相等——v1 前 252 轮均值 148,180 vs v2 全程均值 145,077（差 2.1%）；同指数 cache_read 差仅 160,080（=0.5%）。分bucket 对照：r26-50（61.9K/61.6K）、r151-175（187.4K/180.6K）、r201-225（231.1K/231.6K）——v2 中段并不更低。
- **成立部分**：平均 prefix −26.5% 是**组合效应**——v1 后 103 轮均值 prefix **317,419**（=1.64×全程均值 193,426），v2 根本没跑进 300-400K 尾部。尾部规避 cache = Σp1[253:355] = **32,694,192 = cache 削减的 99.5%**（同指数差 0.5%）。被避开尾部轮单轮 EQ ≈ **91,258** vs 均值轮 55,610（1.64×）。
- **裁决**：超额收割（EQ −47.7% vs 轮次 −29%）= 真机制价值，机制是「**轮次收割在 prefix 最贵的尾部兑现**」（cache=Σprefix 的乘法结构），而非「每轮内容变小」。池模型按均值轮计价 → 系统性低估 ~1.64×，这就是三代预测带全破下沿的原因。

**2c prefix 差来源拆解（回答「−26.5% 从哪来」：不是组件瘦身，是组合）**

| 项 | 实测 | EQ 量级 |
|---|---|---|
| V-B 模块瘦身 | 同指数 cr 差仅 160,080（m2 21,453→20,231c 在内） | ≈46K（0.5%） |
| manage 轮消除 | v1 Task 轮 52（全轮成本 2,912,366 EQ）；v2 Task=0 | 轮桶归属，见下 |
| ③④⑤ 包装消除 | 两 transcript multi_tool_use 轮均=0——烘焙链合并的是命令包装，效应=轮数减少 | 同上 |
| scope（Phase 2/1.5） | → 段三 | ≈0.53M |

103 被避轮的桶分解（归属口径，成本不可加总——顺序依赖）：**Task/manage 52 + scope ≈12 + 写作/取数效率 ≈39**（烘焙链/批取/零 fetch 补救/零 websearch）。

## ③ scope 可比性裁决

**3a Phase 1.5/2 对称性精算（只加）**：基线确实执行（xq_voice@r37/315、precheck@r39）；v2 跳过（precheck@r11）。实测追加：

| 项 | 量 | 证据 |
|---|---|---|
| Phase 2/1.5 执行轮成本差 | 357,359 | 基线 r25-39 全轮 429,780 − v2 r8-11 72,421 |
| Phase 4.5 xq 重拉轮 | 102,146 | 基线 xq_voice@315 单轮全成本 |
| 拉取内容驻留 | **72,326** | r25-39 result 仅 2,826c——runner/xq 输出走**文件重定向**，不驻留 context（用户粗估 0.4-0.8M 据此修正） |
| websearch 范围差 | ≈626,000 | 基线 12 次搜索调用（audit 归因 5.6% char·turn），v2=0；C4 条件性内容未触发，保守计入 |
| **scope 追加合计** | **≈1,157,831** | |

**3b 修正后主判据复算（终判）**：修正 EQ = 11,927,986 + 1,157,831 ≈ **13,085,817 ≈ 13.09M**。
→ vs 主判据线 15.9709M：**PASS，余量 18.0%**（对基线 0.574×，修正后削减 42.6%）。未落中间带（15.97-18.25M），复跑协议不触发。**总裁决：scope 修正后主判据 PASS——总裁决的安全垫方向被证实（13.09M 落在其粗估 13.0-13.5M 带内）。**

**3c 取数对账（v2 49,813c vs 基线 71,498c，−29.5%）**：调用 59→40 次（−32%）；链式调用轮 3→1。三分：**合法去重**（重复读减少）为主力；**批取合并**贡献小（③④⑤ 合并的是命令包装，链式视图轮 3→1）；**scope 缩减**（websearch findings 消费 15 src + Phase2 邻接读）。数据合同满足链：sidecar data_coverage 11/11 + 64 门 fixtures 漏报 0 + 27 verify 0 FAIL。

## ④ 程序合规清单

**4a 时序三查**（git `%ci` 直查）：
1. 冻结 commit 39e1454 = 2026-09-18 **01:22:54** +0800 < 判据票首条消息 **12:22:12** +0800 → 冻结前置 11h ✅
2. V1 价表侧=冻结同日同 commit（双日双抓入冻结条目）→ 合规；V1 账单侧冻结时「挂起」**系显式声明**（§C.5 在案，非隐瞒），本审计 §1c 补围界闭环 → 轻程序项已闭环
3. V-B b8b6966（02:18）/ V-M 6d7b2da 均早于发射 ✅
4. 002851 并行会话隔离：v2 transcript 引用 c2c1ab4e=0、002851=0；/tmp 全部 601208-scoped（清单模板占位串除外）✅；T15 错挂拦截经审计器零写入拒绝兑现
5. 轻注记：`tdx_publish prepare` 本地步被执行（publish.mdx 产出、**未上传**）——「跳发布」语义下超范围轻执行，入账

**4b 账本六笔**：
1. **T15 修复-生效闭环首例**：上批引擎修（3f96947）在本票审计时活体拦截真实跨会话错挂（审计器对 c2c1ab4e/002851 零写入拒绝）——trap_ledger 体系首个「修复在下一票被活体验证」完整闭环。
2. **V-B 未命中零加载首验**：本票触发态全不在场（caliber=None/_profile=normal/sgr 适用/三维 disclosed_ok），m2-cond 零读取，m2 首读 20,231c（基线 21,453c）——「触发才读」语义生产验证。
3. **V-D 烘焙链含 T2 首次生产执法**：27 verify 全过、gate 失败阻断勾选路径未触发（无 FAIL 可阻），管理轮禁令生效（Task 52→0，清单唯一状态）。
4. **W6 隐性推理 73% 带上沿**：output 252,719 中可见仅 ≈67K，隐性 ≈185.7K（推算±30%，gateway 不回 thinking 明细）→ v3 主战场（3.5 权重）。
5. **「一阶→二阶」方法论教训**：池模型按均值轮计价系统性低估 1.64×（尾部轮 91.3K vs 均值 55.6K）；cache 削减 99.5% 来自尾部规避——预测带全破下沿的原因不是运气，是一阶近似在乘法结构下的必然偏差。
6. **溯源对照修正**：实测基线 sidecar snapshot_tags=**171**（直接计数 171，双源一致）vs v2 **197**（双源一致）→ v2 **+26（+15%）**；「197 vs 200（−3）」前提不成立。v2 缺 web_research_findings ×15（对应 websearch 范围差，§3a 已计价）。

**4c 全格式要件**：引擎钉 orchestrator `6d7b2da`→`964e09c`（结案 commit）/ quality `b8b6966`；归档 `/home/ubuntu/analysis_report/analysis_report-glm5.3-flash-东材科技-modeA-601208/`（报告 83,920B + sidecar self_score=100 + publish 副本 + 快照 cp -p）；transcript `831500d6`；重放命令见 §⑧。

## ⑤ 结案三态表

| 面 | 判定 | 依据 |
|---|---|---|
| 主判据（scope 修正后） | ✅ **PASS** | 13.09M ≤ 15.9709M（余量 18.0%；对基线 0.574×） |
| 质量硬线 | ✅ | 27 verify 0 FAIL / 修复轮 0 / 删档 0 / 长度 83,920B 带内 / R9 过 / self_score=100 |
| 程序合规 | ✅ | 冻结前置 11h；V1 账单侧挂起系显式声明+本次闭环；两笔轻注记（prepare 超范围、Phase 4.5 消费归档 check） |

**三面全绿 → 可销案。** 注记（不阻断）：① v2 的 xqcheck 消费归档 check 结果（未对本票新结论现跑求证）——scope 语义瑕疵，G80 臂形式合规，质量影响评估留抽检；② 隐性推理 73% 为推算口径。

## ⑥ v3 立项材料

- **output 侧（头号战场）**：隐性 ≈185.7K tokens × 3.5 ≈ **650K EQ** 未收割（73% 推算带）；v2 每轮产出 1,003 tok vs v1 1,372（−27%）已自然缩，进一步靠短思考/批取。
- **prefix 空间**：同指数差仅 0.5%——每轮内容压缩已尽，v3 不应再押「内容后置」。
- **轮次余量**：≈39 未分类轮（写作/取数效率）+ scope 类不可再省。
- **v3 新基线**：修正后 **13.09M**（v2 修正削减 42.6%）。

## ⑦ 判据 v5.1 候选

1. **二阶池模型**：r 口径（轮次）与 EQ 口径强制分列；cache 削减 = 轮次收割 × 尾部权重（实测 1.64×）× output/input 同缩——三条映射系数入模。
2. **预测带重导规则**：EQ 带禁由 r 带线性映射（v5.0 的 16-18M 带即因此全破下沿）；须过 prefix 轨迹结构换算。
3. **W6 升监控线**（非执法线）：gateway 不回 thinking 明细，隐性推理只能推算——设「output − 可见内容估算」水位监控，不作 FAIL 依据。

## ⑧ 重放命令附录

```bash
# 三元组独立复算（任一 transcript）
python3 -c "
import json; s=dict(i=0,c=0,o=0,n=0)
for l in open('/home/ubuntu/.claude/projects/-home-ubuntu/831500d6-0e12-40cf-acbe-76df30de792e.jsonl'):
    r=json.loads(l)
    if r.get('type')=='assistant':
        u=(r.get('message') or {}).get('usage')
        if u: s['n']+=1; s['i']+=u['input_tokens']; s['c']+=u['cache_read_input_tokens']; s['o']+=u['output_tokens']
print(s)"   # → {i:747565, c:35811840, o:252719, n:252}

# EQ 复算
python3 -c "print(747565+0.2875*35811840+3.5*252719)"   # → 11927985.5

# V1 围界（窗口会话 H1 求和 + 双时点互洽）：见审计脚本 §1c（glob projects/*/*.jsonl，ts∈[09:53,14:53]）
# scope 窗口：基线 r25-39 全轮成本 429,780 / v2 r8-11 72,421 / xq@315 102,146 / 专块驻留 72,326
# 审计件：analysis_report-glm5.3-flash-东材科技-modeA-601208/token_audit-601208-20260918-1301.md
```

**审计纪律节**：零写入（本件唯一例外）；scope 修正只加（websearch 0.63M 系判断性保守计入，剔除则修正 EQ=12.46M，仍 PASS——两种口径均在）；二阶「内容后置」假说已证伪、「尾部复合」成立，如实入账；归因未翻转为 scope 主导（scope 追加 1.16M << EQ 削减 10.89M），PASS 维持。

## ⑨ 主会话复验记（2026-09-18，独立方法重算）

- 三元组：自建解析器逐 record 累加 usage，v1/v2 双票差异 **0/0/0**（n=355/252）✅
- EQ 逐位 11,927,985.5 ✅；分解闭合差 **0.0**（轮次 5,727,818 + prefix 3,717,786 = 9,445,603）；总削减 10,887,617 ✅
- 尾部复合独立重现：v1 prefix（input+cache_read+cache_creation 逐轮）前252均值 **148,180** / 尾103均值 **317,419** / 尾部规避 32,694,192 = cache 削减的 **99.5%** ✅；同指数 v1前252 vs v2全程 = **-2.1%** → 「内容后置」证伪、「尾部复合」成立 ✅
- Task\* 轮 52→0 ✅；时序（冻结 01:22:54 < 首条消息 12:22:12）✅；溯源双 sidecar（基线 171 / v2 197）✅
- 注记：①Σprefix = canon cache + canon input（70,035,644 = 68,666,112+1,369,532），「1.64×」按 cache/N 分母、「1.61×」按 Σprompt/N 分母，两者皆可、结论不变；②修正 EQ 13,085,817（余量 18.0%、0.574×基线）与敏感性 12.46M 复算一致 ✅
- 复验结论：承重数字 **7/7 独立确认**，结案件可呈用户终裁。
